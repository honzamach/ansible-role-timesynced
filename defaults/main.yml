---

hm_timesynced__install_packages:
  debian:
    apt:
      - ntp

hm_timesynced__ntp_servers:
  - { name: "server 0.debian.pool.ntp.org" }
  - { name: "server 1.debian.pool.ntp.org" }
  - { name: "server 2.debian.pool.ntp.org" }
  - { name: "server 3.debian.pool.ntp.org" }
